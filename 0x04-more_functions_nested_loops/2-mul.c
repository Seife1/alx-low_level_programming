#include "main.h"
#include <stdio.h>

/**
 * mul - find mul of two numbers
 * @a: first number
 * @b: Second number
 * Return: Always 0.
 *
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
