#include <stdio.h>

/**
 * main - Entry point
 * @argc: Number of argunment
 * @argv: Argunments
 * Return: Always
 */

int main(int argc, char *argv[])
{
	(void) argv;
	printf("%d\n", argc - 1);
	return (0);
}
