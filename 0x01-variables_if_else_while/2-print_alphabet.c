#include <stdio.h>

/**
 * main - entry point
 * Return: Always 0.
 */

int main(void)
{
	int i;
	/* print lowercase alphabet*/

	for (i = 'a'; i <= 'z'; i++)
	{
	putchar(i);
	}
	putchar('\n');

	return (0);
}
